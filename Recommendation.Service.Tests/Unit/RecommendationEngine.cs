using Microsoft.VisualStudio.TestTools.UnitTesting;
using System.Collections.Generic;
using Moq;
using System.Threading.Tasks;

namespace Recommendation.Service.Tests.Unit
{
    [TestClass]
    public class RecommendationEngine
    {
        //[TestMethod]
        //public async Task FindOutStuff()
        //{
        //    var dbContext = new Mock<Database.DatabaseContext>();
        //    var engine = new Service.RecommendationEngine(dbContext.Object);
        //    var stuff = await engine.FindOutStuff(1, new List<int>());
        //}
    }
}
